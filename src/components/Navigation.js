import React from "react";
import { Link } from "react-router-dom";

//Struggled with Navigation, could separate it from Header, so included header with nav already

function Navigation() {
  return (
  <div>

    
  

    </div>
  );
}

export default Navigation;